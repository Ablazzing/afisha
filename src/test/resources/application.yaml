spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/afisha_test?currentSchema=application
    password: terrrr
    username: postgres

logging:
  level:
    org:
      springframework:
        jdbc:
          core:
            JdbcTemplate: DEBUG